name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
      
      - name: Install dependencies
        run: |
          # Install OpenCV 4 (opencv@4) instead of opencv (latest = 5.x)
          # gocv v0.43.0 requires OpenCV 4.x
          brew install opencv@4 tesseract
      
      - name: Build
        run: |
          # Suppress deprecation warnings for NSSpeechSynthesizer
          # (macOS 14+ deprecated it in favor of AVSpeechSynthesizer, but we still support macOS 10.13+)
          export CGO_CFLAGS="-Wno-deprecated-declarations"
          # Set PKG_CONFIG_PATH for OpenCV 4 (Homebrew on Apple Silicon uses /opt/homebrew)
          export PKG_CONFIG_PATH="$(brew --prefix opencv@4)/lib/pkgconfig:$PKG_CONFIG_PATH"
          go build -o cardsense-gui cmd/gui/main.go
      
      - name: Test
        run: |
          go test ./pkg/...
      
      - name: Smoke test
        run: |
          ./cardsense-gui --help || echo "GUI binary exists"
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cardsense-macos
          path: cardsense-gui

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
      
      - name: Install MinGW (C compiler for CGO)
        run: |
          choco install mingw -y
        shell: powershell
      
      - name: Install OpenCV via Chocolatey
        run: |
          choco install opencv -y
        shell: powershell
      
      - name: Install Tesseract
        run: |
          choco install tesseract -y
        shell: powershell
      
      - name: Set up OpenCV environment
        run: |
          # Add MinGW to PATH (installed by Chocolatey)
          echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $GITHUB_PATH
          echo "CGO_CPPFLAGS=-IC:/tools/opencv/build/include" >> $GITHUB_ENV
          echo "CGO_LDFLAGS=-LC:/tools/opencv/build/x64/vc16/lib" >> $GITHUB_ENV
          echo "PATH=C:/tools/opencv/build/x64/vc16/bin;$PATH" >> $GITHUB_ENV
        shell: bash
      
      - name: Build
        run: |
          # Add MinGW to PATH (Chocolatey installs to C:\ProgramData\mingw64\mingw64\bin)
          $env:PATH = "C:\ProgramData\mingw64\mingw64\bin;$env:PATH"
          go build -o cardsense-gui.exe cmd/gui/main.go
        env:
          CGO_ENABLED: 1
          CGO_CPPFLAGS: -IC:/tools/opencv/build/include
          CGO_LDFLAGS: -LC:/tools/opencv/build/x64/vc16/lib
        shell: powershell
      
      - name: Test
        run: |
          go test ./pkg/...
        shell: powershell
      
      - name: Smoke test
        run: |
          if (Test-Path cardsense-gui.exe) {
            Write-Output "✅ cardsense-gui.exe exists"
            $size = (Get-Item cardsense-gui.exe).Length / 1MB
            Write-Output "Size: $($size.ToString('F2')) MB"
          } else {
            Write-Error "❌ cardsense-gui.exe not found"
            exit 1
          }
        shell: powershell
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cardsense-windows
          path: cardsense-gui.exe

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
      - uses: actions/download-artifact@v4
      
      - name: Display structure
        run: ls -R
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            cardsense-macos/cardsense-gui
            cardsense-windows/cardsense-gui.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
